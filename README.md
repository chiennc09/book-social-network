# Book Social Network
The book-social-network project

# Install Kafka
`docker-compose up -d`

# build
`docker-compose up -d redis recommendation-service`

# rebuild
`docker-compose up -d --build recommendation-service`

