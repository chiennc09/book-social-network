server:
  port: 8086
  servlet:
    context-path: /chat
spring:
  application:
    name: chat-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/chat-service?authSource=admin
      auto-index-creation: true
app:
  services:
    profile:
      url: http://localhost:8091/profile
